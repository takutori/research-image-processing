experiment_name: oxford_pet_zeroshot
model_name: microsoft/Florence-2-base-ft
dataset_root: /workspace/data/oxford_iiit_pet
output_dir: /workspace/checkpoints/classification/florence2/oxford_pet_zeroshot

split:
  valid_size: 0.2
  random_state: 42

eval:
  max_new_tokens: 32

prompt: "What breed of pet is shown in the image? Choose exactly one from this list: {labels}"
