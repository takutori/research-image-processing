experiment_name: oxford_pet
model_name: eva02_base_patch14_224
dataset_root: /workspace/data/oxford_iiit_pet
output_dir: /workspace/checkpoints/classification/eva02/oxford_pet
tuning_mode: full

train:
  image_size: 224
  eval_resize_size: 224
  batch_size: 32
  num_workers: 0
  num_epochs: 12
  learning_rate: 0.0001
  weight_decay: 0.0001
  valid_size: 0.2
  random_state: 42
  use_amp: true

grid:
  learning_rates: [0.00003, 0.0001]
  weight_decays: [0.0001, 0.001]
  batch_sizes: [16, 32]
  image_sizes: [224]
  num_epochs: 12
  num_workers: 0

optuna:
  n_trials: 40
  num_epochs: 12
  num_workers: 0
  batch_size_candidates: null
  image_size_candidates: [224]
