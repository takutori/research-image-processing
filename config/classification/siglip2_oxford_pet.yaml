experiment_name: oxford_pet
model_name: google/siglip2-base-patch16-224
dataset_root: /workspace/data/oxford_iiit_pet
output_dir: /workspace/checkpoints/classification/siglip2/oxford_pet
tuning_mode: full

train:
  image_size: 224
  eval_resize_size: 224
  batch_size: 24
  num_workers: 0
  num_epochs: 10
  learning_rate: 0.00005
  weight_decay: 0.0001
  valid_size: 0.2
  random_state: 42
  use_amp: true

grid:
  learning_rates: [0.00003, 0.00005]
  weight_decays: [0.0001, 0.001]
  batch_sizes: [16, 24]
  image_sizes: [224]
  num_epochs: 5
  num_workers: 0

optuna:
  n_trials: 30
  num_epochs: 5
  num_workers: 0
  batch_size_candidates: null
  image_size_candidates: [224]
