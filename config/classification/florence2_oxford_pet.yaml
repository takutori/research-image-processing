experiment_name: oxford_pet
model_name: microsoft/Florence-2-base-ft
dataset_root: /workspace/data/oxford_iiit_pet
output_dir: /workspace/checkpoints/classification/florence2/oxford_pet
tuning_mode: full

train:
  image_size: 224
  eval_resize_size: 224
  batch_size: 4
  num_workers: 0
  num_epochs: 6
  learning_rate: 0.00005
  weight_decay: 0.0001
  valid_size: 0.2
  random_state: 42
  use_amp: true

grid:
  learning_rates: [0.00003, 0.00005]
  weight_decays: [0.0001, 0.001]
  batch_sizes: [2, 4]
  image_sizes: [224]
  num_epochs: 4
  num_workers: 0

optuna:
  n_trials: 20
  num_epochs: 4
  num_workers: 0
  batch_size_candidates: null
  image_size_candidates: [224]
