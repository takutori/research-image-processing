# Repository-local Codex defaults
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[profiles.default]
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[features]
multi_agent = true
shell_tool = true
shell_snapshot = true
personality = true
